# Anthropic API anahtarı — gerçek değeri ASLA bu dosyaya yazma.
# Yerelde:  cp .env.example .env  ve .env içine yaz (.env gitignore'da).
# Canlıda:  Vercel / Railway ortam değişkeni olarak tanımla.
ANTHROPIC_API_KEY=
